//Given an unsorted array of N elements and an element X. The task is to write a recursive function to check whether the element X is present in the given array or not.
//Eg:array[] = {1, 2, 3, 4, 5}
/*X = 3.

The function should return True, as 3 is 
present in the array.*/

public class RecursiveSearchInArray {

    public static void main(String[] args) {
	// write your code here\
        int arr[]={4,6,7,8};
        System.out.println("Factorial"+ recursiveSearch(arr,0,0,6));

    }
    //time complexity= O(N)
    //Space complexity= O(N) for all those space on stack for function calls
    public static boolean recursiveSearch(int arr[], int lower, int upper, int x){
        if (upper<lower){
            return true;
        }
        if (arr[lower]==x){
            return true;
        }
        if (arr[upper]==x){
            return true;
        }
        return recursiveSearch(arr,lower+1, upper-1, x);
    }
}
